name: Update README with activity

on:
  schedule:
    - cron: "0 * * * *"
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Update README Activity
        uses: Readme-Workflows/recent-activity@v2
        with:
          GH_USERNAME: amit-ashok-swain
